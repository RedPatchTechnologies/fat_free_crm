- if (controller.controller_name == 'leads') and (template = template_for_current_view).present?
  = render(template, lead: lead)
- elsif (controller.controller_name == 'campaigns') 
  = render 'leads/index_campaign_brief', lead: lead
- else
  -# default view
  = render 'leads/index_brief', lead: lead
